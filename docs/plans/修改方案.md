## 修改计划

完成状态（2026-07-13）：阶段 0–4 已完成；阶段 5 按原计划继续延期，等待扩展包 metadata 稳定。

整体按独立提交推进，每一步都保持测试通过，避免把依赖替换和领域行为修改混在一起。

### 阶段 0：提交当前 todo-list Zod 改动

当前工作区已有：

- todo-list 输入输出改用 Zod
- 新增 Zod 依赖和锁文件
- 新增行为测试
- 47 个测试通过

建议先单独提交：

```text
refactor: validate todo-list services with zod
```

### 阶段 1：用 Zod校验核心加载 seam

新增内部校验模块，例如 `src/validation.ts`，提供少量接口：

```ts
parseSiteDefinition(value)
parseThemeDefinition(value)
parsePluginDefinition(value)
parseRuntimeInfo(value)
```

修改位置：

- [extensions.ts](/D:/Projects/node_proj/diitey/src/extensions.ts)：校验动态导入的配置、主题和插件
- [runtime-info.ts](/D:/Projects/node_proj/diitey/src/runtime-info.ts)：替换手写 JSON 字段判断
- [plugins.ts](/D:/Projects/node_proj/diitey/src/plugins.ts)：替换 Action 数值配置检查
- [content.ts](/D:/Projects/node_proj/diitey/src/content.ts)：在兼容性测试覆盖后考虑用 Zod ISO schema 校验 `created`

仍然保留手写的领域校验：

- 重复插件服务和 Action
- Action 引用不存在的插件服务
- 路由冲突和 canonical URL
- 集合、内容记录之间的关系

不把 `ValueSchema` 改成 Zod 专属类型，插件仍可使用 Valibot、TypeBox 或自己的 `parse()` 实现。

验收测试：

- 无效 `site.config.ts` 在加载时给出字段路径
- 无效主题和插件结构在打开数据库前失败
- 损坏的 runtime info 被拒绝
- 非法 Action limit、timeout、rate limit 被拒绝

提交：

```text
refactor: validate runtime definitions with zod
```

### 阶段 2：用 `lru-cache` 限制限流状态

添加依赖：

```text
lru-cache
```

新增 `src/rate-limit.ts`，对服务器暴露：

```ts
createActionRateLimiter(options)
rateLimiter.consume(key, policy)
```

实现要求：

- 限制最大键数量，例如 10,000
- 每个键按照 `windowMs` 设置 TTL
- 到期键自动失效
- 不因普通读取延长限流窗口
- 不改变现有每 IP、每 Action 的固定窗口语义

修改 [server.ts](/D:/Projects/node_proj/diitey/src/server.ts)，删除无限增长的 Map 和 `consumeRateLimit()`。

验收测试：

- 同一键超过限制返回 429
- 窗口到期后恢复
- 不同 Action 独立计数
- 超过最大键数量时淘汰旧键

提交：

```text
fix: bound action rate-limit state
```

### 阶段 3：用 `cookie` 处理 CSRF Cookie

添加依赖：

```text
cookie
```

在 [server.ts](/D:/Projects/node_proj/diitey/src/server.ts) 中：

- 用 `cookie.parse()` 替换 `readCookie()`
- 用 `cookie.serialize()` 生成 CSRF Cookie
- 明确设置 `Path=/`
- 明确设置 `SameSite=Strict`
- HTTPS 时设置 `Secure`
- CSRF token 需要浏览器脚本读取，因此暂不设置 `HttpOnly`

通过 HTTP 行为测试验证，不额外创建只有两行转发逻辑的浅模块。

验收测试：

- 编码后的 Cookie 值可以正确读取
- 多 Cookie 请求可以正确解析
- `SameSite`、`Path`、`Secure` 属性正确
- 原有 CSRF 流程继续工作

提交：

```text
refactor: use standard cookie parsing
```

### 阶段 4：用 `picomatch` 提供标准集合 glob

添加依赖：

```text
picomatch
```

修改 [snapshot.ts](/D:/Projects/node_proj/diitey/src/snapshot.ts)：

- 删除手写 `matchesGlob()`
- 在加载主题时预编译集合 glob
- 构建内容快照时复用 matcher，避免为每条内容记录重复创建正则
- 统一把源路径标准化为 `/`

正式支持：

- `*`
- `**`
- `?`
- 字符集合
- brace patterns，例如 `**/*.{md,mdx}`

验收测试：

- 当前所有 glob 行为保持兼容
- globstar、brace、字符集合正常
- Windows 路径分隔符正常
- 非法 glob 在启动时失败

这会扩大主题的集合接口，需要同步 README 和总方案。

提交：

```text
feat: adopt standard collection globs
```

### 阶段 5：可选的扩展版本校验

等扩展包 metadata 进一步确定后再引入 `semver`：

- 校验插件声明的版本
- 校验扩展要求的 Diitey 版本
- 拒绝不兼容扩展
- 检测插件代码版本降级

当前先不做，避免在包 manifest 尚未稳定时提前固化接口。

### 暂不修改

- 不引入 Hono、Elysia 等 HTTP 框架
- 不用 `path-to-regexp` 替换路由映射
- 不用 `gray-matter` 替换当前严格 YAML 解析
- 不用 `p-timeout` 替换带 AbortSignal 的插件超时
- 不为简单 HTML 转义增加依赖

每个阶段完成后执行：

```text
bun run typecheck
bun test
git diff --check
```
