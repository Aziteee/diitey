# 服务启动前自动执行插件迁移

Diitey 在主题、插件、初始内容快照和 islands 全部校验成功后，于 HTTP 监听开始前在一个事务中自动应用所有待处理插件迁移；内容 `reload` 不执行迁移。相比由 Diitey CLI 管理插件安装与迁移，这让站点所有者直接使用 Bun 和 `package.json` 管理扩展，并减少本地单进程部署的运维步骤；代价是启动会修改 SQLite，因此任一迁移失败必须整体回滚并终止启动，已执行迁移的校验和变化和数据库版本高于插件支持版本仍必须拒绝运行。
