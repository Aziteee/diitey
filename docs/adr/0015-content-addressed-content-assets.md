# 以内容寻址的磁盘产物发布内容资源

核心只发布由内容文件原生 Markdown 引用、且能解析到 `content/` 内普通非 Markdown 文件的内容资源；构建时将引用改写为 `/assets/content/<sha256>/<原文件名>`，并由内容快照索引核心管理的不可变磁盘产物，无法解析的引用保持原样。相比按源路径直接读取文件或把字节装入内存，内容寻址允许大型资源流式响应、复用产物和长期 immutable 缓存，同时保持资源只在成功 reload 后随有效发布视图原子生效。
