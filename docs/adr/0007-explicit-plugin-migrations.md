---
status: superseded by ADR-0008
---

# 插件迁移必须显式执行

插件安装或升级通过 CLI 显式执行带版本、校验和与事务记录的迁移，服务启动和内容 `reload` 只检查兼容性，不自动修改数据库。相比启动时自动迁移，这会增加一次运维步骤，却避免服务启动隐式执行不可逆写入，并能在代码与数据库 schema 不兼容时明确拒绝运行；已执行迁移不得静默改写或重跑，回退依赖插件提供的向下迁移或备份恢复。
