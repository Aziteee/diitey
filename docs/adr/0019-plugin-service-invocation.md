# 插件可通过核心在进程内调用其它插件服务

插件服务执行上下文提供进程内的插件服务调用（`call` 返回 Promise），使一个已加载插件能 await 另一插件声明的服务。调用面向已注册的服务名，共享父调用的取消信号，限制调用深度，且不转发请求元数据；未知服务由调用方处理。相比事件总线或插件直接 import 彼此实现，这保持类型化服务为唯一受支持边界，并避免把出站能力做成 public Action。主题与插件仍是可信本地代码（ADR-0006）：插件服务调用是协作机制，不是隔离沙箱。
