# 以原子快照发布内容

线上请求只读取一份完整、只读的有效发布视图，其内容部分来自内容快照（内容记录与索引）。内容变更由显式 `reload` 在固定站点程序下重建内容快照，并在全部校验成功后原子替换有效发布视图；失败时继续使用上一份有效发布视图。相比请求时读取文件或文件变化后增量修改共享索引，这提高了重建成本并要求显式刷新，但保证请求期间视图一致，也防止编辑半成品、解析错误或扩展异常破坏正在运行的站点。
